# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.14.0
pgvector>=0.3.5

# Redis
redis>=5.2.0

# HTTP / Crawling
aiohttp>=3.11.0
aiohttp-socks>=0.9.0
beautifulsoup4>=4.12.0
playwright>=1.49.0

# LLM (all optional — only install what you need)
# ollama is accessed via HTTP, no package needed

# PDF Reports
reportlab>=4.1.0

# Notifications
aiosmtplib>=3.0.0

# Auth
argon2-cffi>=25.1.0
PyJWT>=2.9.0
cryptography>=43.0.0

# Geo / Feeds
geoip2>=4.8.0
websockets>=12.0

# Utils
python-dotenv>=1.0.0
